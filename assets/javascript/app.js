$(document).ready(function(){	

	var dads = ['Phil Dunpy', 'George Father of the Bride', 'Ray Barone', 'Darth Vader', 'Michael Bluth', 'Mufasa', 'Red Forman', 'Dr. Evil', 'Danny Tanner'];

	function initialize() {
		for (var i = 0; i < dads.length; i++) {
			// var button = $('<div>');
			// $(button).text(dads[i]);
			// $('#dad-buttons').html(button);
			console.log(dads[i]);
		};
	};

	initialize();
});